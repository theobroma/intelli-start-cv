export type ProjectType = {
  codeLink: string;
  demoLink: string;
  desc: string;
  thumbnail: string;
  title: string;
};

export type LinksType = {
  title: string;
  path: string;
};

export type Anchor = 'top' | 'left' | 'bottom' | 'right';
